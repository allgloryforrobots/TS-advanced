const message: string = "What is your name?";

const map = new Map()

const button = document.querySelector('#btn')

if (button) {
    button.addEventListener('click', () => {
        console.log(1)
    })
}
